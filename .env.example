# Copy this file to .env and fill in real values. Never commit .env.
SECRET_KEY=replace-with-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost,your-api-domain.com

# MySQL
DB_NAME=devsphere_lms
DB_USER=root
DB_PASSWORD= khuzaifah00479.
DB_HOST=127.0.0.1
DB_PORT=3306

# JWT
JWT_ACCESS_HOURS=6
JWT_REFRESH_DAYS=7

# CORS - the origins allowed to call this API from a browser
CORS_ALLOWED_ORIGINS=https://devspherelms.vercel.app,http://127.0.0.1:5500

# Library business rules (match the original frontend defaults)
FINE_RATE_PER_DAY=1000
BORROW_PERIOD_DAYS=7
DUE_SOON_REMINDER_DAYS=2
TIME_ZONE=Africa/Kampala
